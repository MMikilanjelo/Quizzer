enabled: true

nameOverride: ""
fullnameOverride: ""

replicaCount: 1

image:
  repository: ghcr.io/example/activity-service
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets: [ ]

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

config:
  ASPNETCORE_ENVIRONMENT: "Production"

appSecret:
  existingSecretName: ""
  data: { }

postgres:
  host: ""
  port: "5432"
  name: ""
  user: ""
  passwordSecretName: ""
  passwordSecretKey: "password"

kafka:
  bootstrapServers: ""
  userName: ""
  passwordSecretName: ""
  passwordSecretKey: "password"
  createUser: true
